# Scripts
This repository holds all of the scripts that I have created or generated to help automate small/repetitive tasks.

## Description of shell scripts in this repository
### refresh-yubikey-agent.sh
This script is used to refresh the identities loaded in ssh-agent. This can be loaded on terminal startup, or can be executed at machine startup to automatically refresh yubikey identity in ssh-agent.
